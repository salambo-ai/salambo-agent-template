# Agent workspace

Files in this directory seed `/workspace` when a new run starts.

Use this area for starter documents, templates, sample inputs, or expected
output folders. Do not store credentials here. A durable follow-up turn restores
the run's saved workspace instead of copying these files again.
